"""Worker.

Fase 1 (agente D) añade aquí: scheduler.py (APScheduler BlockingScheduler,
run inmediato al arrancar). Se ejecuta con `python -m app.worker.scheduler`.
No crear módulos internos en Fase 0.
"""
